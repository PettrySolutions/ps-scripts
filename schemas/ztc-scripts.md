# Goal 

    Setup a CLI library that will provide different tools that are each modular and distict. The first tool will be called import-mapping which we can leave blank for now.

## Requirements

Use Typescript and Node and setup our package so that we can deploy this to github packages under my current account. 

Each modular tool should have full test coverage and developed with functional programming principles.